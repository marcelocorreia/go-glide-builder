resources:
  - name: goglidebuilder
    type: git
    source:
      uri: https://github.com/marcelocorreia/go-glide-builder.git
      branch: master

jobs:
  - name: build
    plan:
      - get: goglidebuilder
        trigger: true

      - task: build

        file: "./ci/build.yml"
